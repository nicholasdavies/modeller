% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_model}
\alias{fit_model}
\title{Fit a model to data}
\usage{
fit_model(model, objective, theta, lower, upper, data, maxit = 500)
}
\arguments{
\item{model}{A model object created by \code{\link[=ode_model]{ode_model()}} or \code{\link[=ssa_model]{ssa_model()}}.}

\item{objective}{A function with signature \verb{function(model, theta, data)}
that returns a numeric vector of pointwise log-likelihoods (or any
quantity to be summed and maximised).}

\item{theta}{Numeric vector of initial parameter guesses. Must lie
strictly between \code{lower} and \code{upper} (not on the bounds).}

\item{lower}{Numeric vector of lower bounds, the same length as \code{theta}.
Use \code{-Inf} for unbounded below.}

\item{upper}{Numeric vector of upper bounds, the same length as \code{theta}.
Use \code{Inf} for unbounded above.}

\item{data}{Data to pass to \code{objective} (typically a data frame of
observations).}

\item{maxit}{Maximum number of iterations (default 500).}
}
\value{
A list with components:
\describe{
\item{theta}{Fitted parameter values on the original scale.}
\item{value}{Maximised objective (sum of values returned by
\code{objective}).}
\item{iterations}{Number of function evaluations.}
\item{convergence}{Character string describing convergence status.}
}
}
\description{
Fits model parameters by minimising the negative sum of values returned
by \code{objective}, using Nelder-Mead optimisation via \code{\link[=optim]{optim()}}. Parameters
are internally transformed to an unbounded scale so that box constraints
are respected without requiring a bounded optimiser.
}
