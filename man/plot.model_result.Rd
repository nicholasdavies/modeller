% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.model_result}
\alias{plot.model_result}
\title{Plot model results}
\usage{
\method{plot}{model_result}(
  x,
  series = NULL,
  overlay = NULL,
  palette = "Dark2",
  legend = "right",
  time_col = "t",
  vline = NULL,
  title = NULL,
  xlab = "Time",
  ylab = NULL,
  compare = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{model_result} object returned by \code{\link[=run_model]{run_model()}}.}

\item{series}{Character vector of series names to display. \code{NULL} (the
default) shows all compartments and incidence series.}

\item{overlay}{Optional data frame of observed data to overlay as points.
Should contain a time column and one or more columns matching
compartment or incidence names.}

\item{palette}{Colour palette: a palette name (e.g. \code{"Dark2"}, \code{"Okabe-Ito"},
\code{"viridis"}) or a function that takes \code{n} and returns \code{n} colours.}

\item{legend}{Legend position: \code{"right"}, \code{"left"}, \code{"top"}, \code{"bottom"},
or \code{"none"}.}

\item{time_col}{Name of the time column in \code{overlay}. Default \code{"t"}.}

\item{vline}{Optional x-intercept for a vertical reference line.}

\item{compare}{Optional second \code{model_result} to draw underneath in
thinner, fainter lines for visual comparison.}

\item{...}{Ignored.}
}
\value{
The \link[ggplot2:ggplot]{ggplot2::ggplot} object (returned by \code{print()}).
}
\description{
Plots compartment dynamics from a model simulation. Overlay data points
can be added for comparison with observed data.
}
\examples{
m = ode_model(
    init = list(S = 999, I = 1, R = 0),
    params = list(beta = 0.3, gamma = 0.1, time = 50),
    equations = function() {
        N = S + I + R
        d(S) = -beta * I / N * S
        d(I) =  beta * I / N * S - gamma * I
        d(R) =  gamma * I
    }
)
result = run_model(m)
plot(result)
plot(result, series = c("I", "R"), palette = "viridis")

}
